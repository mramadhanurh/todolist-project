// src/store/index.js
import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // Tambahkan state yang diperlukan
  },
  mutations: {
    // Tambahkan mutations
  },
  actions: {
    // Tambahkan actions
  },
  modules: {
    // Tambahkan modul jika diperlukan
  },
});
